---
phase: 02-menu-drawer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HamburgerMenu.tsx
  - src/App.tsx
autonomous: false
requirements:
  - MENU-01
  - MENU-02
  - MENU-03
must_haves:
  truths:
    - "Tapping the hamburger icon slides the drawer in from the left with a visible ease-in-out animation"
    - "Tapping the close button slides the drawer back out to the left (same transition in reverse)"
    - "A dark semi-transparent overlay covers the map and controls while the drawer is open"
    - "Tapping the backdrop (outside the drawer) closes the drawer with the slide-out animation"
    - "When the drawer is closed, the backdrop does not intercept any map or button taps"
  artifacts:
    - path: "src/components/HamburgerMenu.tsx"
      provides: "Left-side drawer panel + semi-transparent backdrop, always mounted, CSS-transition driven"
      contains: "translate-x-0 -translate-x-full transition-transform duration-300 ease-in-out"
    - path: "src/App.tsx"
      provides: "Always-mounted HamburgerMenu with open prop"
      contains: "<HamburgerMenu open={showMenu}"
  key_links:
    - from: "src/App.tsx showMenu state"
      to: "HamburgerMenu open prop"
      via: "open={showMenu} prop pass"
      pattern: "<HamburgerMenu open=\\{showMenu\\}"
    - from: "HamburgerMenu backdrop div"
      to: "onClose handler"
      via: "onClick prop"
      pattern: "onClick=\\{onClose\\}"
---

<objective>
Refactor the HamburgerMenu component from a top-banner overlay to a left-side slide drawer with a dimming backdrop, and update App.tsx to always mount the component (required for exit animation).

Purpose: MENU-01, MENU-02, MENU-03 — users get the expected mobile drawer UX: smooth slide-in/out animation with a semi-transparent backdrop that also serves as a close target.

Output:
- `src/components/HamburgerMenu.tsx` — always-mounted drawer panel + backdrop
- `src/App.tsx` — always renders HamburgerMenu with `open` prop instead of conditional mount
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-menu-drawer/02-RESEARCH.md
@.planning/phases/02-menu-drawer/02-UI-SPEC.md
@.planning/phases/02-menu-drawer/02-VALIDATION.md

<interfaces>
<!-- Current HamburgerMenu.tsx — full file must be replaced -->
<!-- Key facts: -->
<!--   - Props: onClose, onPrivacy, onTrademarks, onCookies (no `open` prop today) -->
<!--   - Backdrop today: fixed inset-0 z-[2900] — NO bg-color, NO transition — just a click target -->
<!--   - Panel today: fixed top-0 left-0 right-0 z-[3000] — top-banner layout, NOT a left-side drawer -->
<!--   - safe-area-inset-top inline style is on line 18 — must be preserved on the drawer panel -->
<!--   - MenuItem subcomponent is defined in the same file — keep it unchanged -->

<!-- Current App.tsx mount of HamburgerMenu (line 112-119): -->
<!--   {showMenu && ( -->
<!--     <HamburgerMenu -->
<!--       onClose={() => setShowMenu(false)} -->
<!--       onPrivacy={() => setShowPrivacy(true)} -->
<!--       onTrademarks={() => setShowTrademark(true)} -->
<!--       onCookies={() => { clearAdConsent(); setShowBanner(true) }} -->
<!--     /> -->
<!--   )} -->
<!-- Must become: -->
<!--   <HamburgerMenu -->
<!--     open={showMenu} -->
<!--     onClose={() => setShowMenu(false)} -->
<!--     onPrivacy={() => setShowPrivacy(true)} -->
<!--     onTrademarks={() => setShowTrademark(true)} -->
<!--     onCookies={() => { clearAdConsent(); setShowBanner(true) }} -->
<!--   /> -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor HamburgerMenu to left-side drawer</name>
  <files>src/components/HamburgerMenu.tsx</files>
  <read_first>
    - src/components/HamburgerMenu.tsx (current implementation — read the full file before editing)
    - .planning/phases/02-menu-drawer/02-UI-SPEC.md (interaction contract, color, spacing, geometry)
    - .planning/phases/02-menu-drawer/02-RESEARCH.md (Architecture Patterns section — always-mounted CSS toggle)
  </read_first>
  <action>
Replace the entire HamburgerMenu.tsx with an always-mounted drawer implementation. Specific changes:

**HamburgerMenuProps interface — add `open: boolean`:**
```typescript
interface HamburgerMenuProps {
  open: boolean
  onClose: () => void
  onPrivacy: () => void
  onTrademarks: () => void
  onCookies: () => void
}
```

**Backdrop div — replace the current featureless click-catcher with a styled, animated overlay:**
```tsx
<div
  className={`fixed inset-0 z-[2900] bg-black/50 transition-opacity duration-300 ease-in-out ${
    open ? 'opacity-100 pointer-events-auto' : 'opacity-0 pointer-events-none'
  }`}
  onClick={onClose}
/>
```
Key changes vs current: adds `bg-black/50`, `transition-opacity duration-300 ease-in-out`, conditional `opacity-100`/`opacity-0`, and conditional `pointer-events-auto`/`pointer-events-none`.

**Drawer panel div — replace current top-banner div with left-side drawer:**
```tsx
<div
  className={`fixed top-0 left-0 h-full z-[3000] w-72 max-w-[80vw] bg-white shadow-xl overflow-y-auto
    transition-transform duration-300 ease-in-out
    ${open ? 'translate-x-0' : '-translate-x-full'}`}
  style={{ paddingTop: 'calc(env(safe-area-inset-top, 0px))' }}
>
```
Key changes vs current: removes `right-0`, adds `h-full`, `w-72 max-w-[80vw]`, `shadow-xl`, `overflow-y-auto`, `transition-transform duration-300 ease-in-out`, and conditional `translate-x-0`/`-translate-x-full`. The inline `paddingTop` style (safe-area-inset-top) is preserved.

**Drawer header — no change to copy or icons.** The header `div` content stays exactly as-is (logo img + "Essence QC" span + X button). Only its parent container changes.

**Menu items — no change.** `MenuItem` component definition stays identical. All item onClick handlers stay identical.

**Do not change:** imports, the `MenuItem` subcomponent signature, `ADS_ENABLED` conditional, the GitHub feedback `<a>` link, aria-labels, copy, icon sizes, or spacing classes on inner elements.

The `border-b border-gray-200` on the outer panel and `border-b border-gray-100` on the header stay as-is — they now define the drawer's right edge and header divider respectively.
  </action>
  <verify>
    <automated>cd /Users/alainrousseau/Dev/prix-essence-quebec && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/HamburgerMenu.tsx` contains `open: boolean` in the `HamburgerMenuProps` interface
    - File contains `translate-x-0` and `-translate-x-full`
    - File contains `transition-transform duration-300 ease-in-out` on the drawer panel
    - File contains `bg-black/50 transition-opacity duration-300 ease-in-out` on the backdrop div
    - File contains `pointer-events-none` (backdrop closed state)
    - File contains `pointer-events-auto` (backdrop open state)
    - File contains `w-72 max-w-[80vw]` (non-full-width requirement MENU-01)
    - File contains `h-full` on the drawer panel
    - File does NOT contain `right-0` on the drawer panel div
    - File still contains `paddingTop: 'calc(env(safe-area-inset-top, 0px))'`
    - `npm run build` exits with code 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>HamburgerMenu accepts `open: boolean`; drawer slides in/out via translate classes; backdrop fades in/out via opacity classes; backdrop blocks pointer events only when open; build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update App.tsx to always mount HamburgerMenu</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (current implementation — read the full file before editing, focus on lines 112-119)
    - src/components/HamburgerMenu.tsx (already updated in Task 1 — confirms `open` prop exists)
  </read_first>
  <action>
In `src/App.tsx`, change the conditional render of `HamburgerMenu` from a mounted-only-when-open pattern to an always-mounted pattern driven by an `open` prop.

**Find this block (lines 112-119):**
```tsx
{showMenu && (
  <HamburgerMenu
    onClose={() => setShowMenu(false)}
    onPrivacy={() => setShowPrivacy(true)}
    onTrademarks={() => setShowTrademark(true)}
    onCookies={() => { clearAdConsent(); setShowBanner(true) }}
  />
)}
```

**Replace with:**
```tsx
<HamburgerMenu
  open={showMenu}
  onClose={() => setShowMenu(false)}
  onPrivacy={() => setShowPrivacy(true)}
  onTrademarks={() => setShowTrademark(true)}
  onCookies={() => { clearAdConsent(); setShowBanner(true) }}
/>
```

This is the only change needed in App.tsx. Remove the `{showMenu && (...)}` wrapper entirely. All prop values stay identical — only `open={showMenu}` is added and the conditional wrapper is removed.

**Do not change** anything else in App.tsx: no state changes, no handler changes, no import changes, no layout changes.
  </action>
  <verify>
    <automated>cd /Users/alainrousseau/Dev/prix-essence-quebec && npm run build && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/App.tsx` contains `<HamburgerMenu` with `open={showMenu}` as a prop (not inside a conditional block)
    - File does NOT contain `{showMenu && (` followed by `<HamburgerMenu`
    - `npm run build` exits with code 0
    - `npm run lint` exits with code 0 (no TypeScript errors, no unused vars)
  </acceptance_criteria>
  <done>HamburgerMenu is always mounted; `open={showMenu}` drives visibility via CSS; exit animation works because the element stays in the DOM when showMenu becomes false; build and lint pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Left-side slide drawer: HamburgerMenu refactored to always-mounted drawer panel + animated backdrop. App.tsx updated to pass `open` prop instead of conditional mount. CSS transitions handle both slide-in (MENU-01) and slide-out (MENU-02) animations. Backdrop (MENU-03) dims screen and serves as a close tap target.
  </what-built>
  <how-to-verify>
    Run `npm run dev` (or `netlify dev`) and open on a mobile viewport (or Chrome DevTools mobile emulation at 390px wide).

    1. **Slide-in (MENU-01):** Tap the hamburger icon (top-left). The drawer should slide in smoothly from the left edge — not snap. Width should be ~288px (not full-screen). A dark semi-transparent overlay should cover the map and controls behind it.

    2. **Slide-out via close button (MENU-02):** With drawer open, tap the X button in the drawer header. The drawer should slide back out to the left smoothly — same duration as slide-in.

    3. **Backdrop tap closes drawer (MENU-03):** Open the drawer again. Tap anywhere on the dark overlay to the right of the drawer. The drawer should slide back out.

    4. **No backdrop interference when closed:** Close the drawer fully. Tap the map, tap the hamburger button, tap filter badges. All should respond normally — the backdrop should NOT be intercepting clicks.

    5. **Menu items still work:** Open drawer, tap "Politique de confidentialité" — Privacy modal should open. Open drawer, tap "Marques & logos" — Trademark modal should open.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe which check failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → drawer | User taps backdrop or close button; both call onClose() — no data input, no auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | HamburgerMenu backdrop onClick | accept | onClick only calls onClose() — a setState setter. No user-controlled data flows through this handler. No injection surface. |
| T-02-02 | Denial of Service | `pointer-events-none` on backdrop when closed | mitigate | Applied via Tailwind class on closed state — backdrop cannot swallow map interactions when drawer is closed. Verified in acceptance criteria. |
| T-02-03 | Information Disclosure | Drawer links (Privacy, Trademarks, GitHub) | accept | All links are existing hardcoded values — no user input, no dynamic URL construction. GitHub link uses `rel="noopener noreferrer"` (already present in current code). |

Note: ASVS L1 V1–V6 categories (authentication, session, access control, input validation, cryptography, errors) do not apply to this phase. The drawer is a purely presentational navigation surface with no data handling.
</threat_model>

<verification>
Run after both tasks complete:

```bash
cd /Users/alainrousseau/Dev/prix-essence-quebec && npm run build && npm run lint
```

Both commands must exit 0.

TypeScript-level checks confirmed by build:
- `open: boolean` prop accepted by HamburgerMenu
- No unused variables introduced
- No implicit `any` types
- `verbatimModuleSyntax` satisfied (no missing `import type`)

Visual verification via checkpoint:human-verify task above.
</verification>

<success_criteria>
1. `npm run build && npm run lint` exits 0 — no TypeScript errors, no lint violations
2. `src/components/HamburgerMenu.tsx` contains `translate-x-0`, `-translate-x-full`, `transition-transform duration-300 ease-in-out`, `bg-black/50`, `w-72 max-w-[80vw]`, `pointer-events-none`
3. `src/App.tsx` contains `open={showMenu}` on HamburgerMenu and does NOT contain `{showMenu &&` before the HamburgerMenu render
4. Drawer slides in from left on open (MENU-01) — human verified
5. Drawer slides out on close button tap (MENU-02) — human verified
6. Backdrop dims screen and closes drawer on tap (MENU-03) — human verified
7. Backdrop does not block interactions when drawer is closed — human verified
</success_criteria>

<output>
After completion, create `.planning/phases/02-menu-drawer/02-01-SUMMARY.md` using the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
