---
phase: 04-adsense-placement-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ADS-STRATEGY.md
autonomous: true
requirements:
  - ADS-01
  - ADS-02
must_haves:
  truths:
    - "A file exists at .planning/ADS-STRATEGY.md"
    - "The document lists exactly two in-scope zones: station card popup (active) and full-page interstitial (planned)"
    - "The document specifies a max density of 1 active ad unit at a time"
    - "Slot ID 3373913657 is mapped to the station card popup zone"
    - "The document states that no ad zone may block the map, price stats bar, or filter controls on mobile"
    - "The document references both activation gates: ADS_ENABLED constant and VITE_ADSENSE_PUBLISHER_ID env var"
    - "Out-of-scope zones (bottom banner, hamburger menu ad, sidebar) are explicitly labelled as not currently acceptable"
  artifacts:
    - path: ".planning/ADS-STRATEGY.md"
      provides: "Developer-facing AdSense placement strategy for Phase 5 implementers"
      min_lines: 80
  key_links:
    - from: ".planning/ADS-STRATEGY.md Zone table"
      to: "src/components/Map.tsx slot ID 3373913657"
      via: "slot ID reference"
      pattern: "3373913657"
    - from: ".planning/ADS-STRATEGY.md Activation Checklist"
      to: "src/lib/adConsent.ts ADS_ENABLED"
      via: "explicit code reference"
      pattern: "ADS_ENABLED"
    - from: ".planning/ADS-STRATEGY.md Activation Checklist"
      to: "src/lib/adConsent.ts VITE_ADSENSE_PUBLISHER_ID"
      via: "explicit env var reference"
      pattern: "VITE_ADSENSE_PUBLISHER_ID"
---

<objective>
Write the AdSense placement strategy document at `.planning/ADS-STRATEGY.md`.

Purpose: Provide a single, authoritative developer reference that Phase 5 executor agents will implement against. All decisions are locked in CONTEXT.md (D-01 through D-07). This task codifies them into a scannable Markdown file.

Output: `.planning/ADS-STRATEGY.md` — a complete, accurate strategy document covering two ad zones, one density policy, consent gating, a Phase 5 activation checklist, and out-of-scope boundaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-adsense-placement-strategy/04-CONTEXT.md
@.planning/phases/04-adsense-placement-strategy/04-RESEARCH.md
@.planning/phases/04-adsense-placement-strategy/04-UI-SPEC.md

<interfaces>
<!-- Key code facts the executor needs. Do not explore the codebase; use these directly. -->

From src/lib/adConsent.ts:
- Line 2: `const ADS_ENABLED = false` — global on/off gate; currently false
- Line 6: `ADSENSE_PUBLISHER_ID` sourced from `import.meta.env.VITE_ADSENSE_PUBLISHER_ID`
- Lines 13-27: `getAdConsent()` returns `'accepted' | 'refused' | null`
- Lines 29-32: `saveConsent()` — 180-day TTL, localStorage key `'ad-consent'`
- Lines 38-51: `loadAdSense()` — deferred async script injection into `<head>`

From src/components/Map.tsx:
- Line 9: `const ADSENSE_CLIENT = import.meta.env.VITE_ADSENSE_PUBLISHER_ID ?? ''`
- Lines 90-101: `adSlotHtml` template — conditional on `ADSENSE_CLIENT.trim()` non-empty
- Line 96: `data-ad-slot="3373913657"` — only active slot in the codebase

From src/index.css (lines 175-182):
```css
.station-card-ad-container {
  display: none;
}
.station-card-ad-container:has(.adsbygoogle[data-ad-status="filled"]) {
  display: block;
}
```

From src/App.tsx (lines 29-30, 36, 162):
```typescript
const initialConsent = getAdConsent()
if (ADS_ENABLED && initialConsent === 'accepted') loadAdSense()
const [showBanner, setShowBanner] = useState(ADS_ENABLED && initialConsent === null)
{showBanner && <ConsentBanner onConsent={handleConsent} />}
```

App layout: `h-dvh flex flex-col` — map fills `flex-1 min-h-0`, PriceStatsBar at bottom. Station card popup (Leaflet tooltip) renders inside the map layer. Filter controls are absolutely positioned at `z-[1001]` top-left.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .planning/ADS-STRATEGY.md</name>
  <files>.planning/ADS-STRATEGY.md</files>

  <read_first>
    - .planning/phases/04-adsense-placement-strategy/04-CONTEXT.md — locked decisions D-01 through D-07 (source of truth for all content)
    - .planning/phases/04-adsense-placement-strategy/04-RESEARCH.md — verified code facts for zone table and pitfall callouts
    - .planning/phases/04-adsense-placement-strategy/04-UI-SPEC.md — binding visual contracts for Zone 1 and Zone 2
    - .planning/REQUIREMENTS.md — ADS-01 and ADS-02 acceptance criteria (drive the document's scope)
  </read_first>

  <action>
Write `.planning/ADS-STRATEGY.md` using the Write tool. The document must follow this exact structure (sections in this order):

**1. Title and metadata block**

```
# AdSense Placement Strategy

**Phase:** 4 — AdSense Placement Strategy
**Document status:** Active — governs Phase 5 implementation
**Created:** 2026-04-19
**Audience:** Developer implementing Phase 5 ad units
```

**2. Section: ## Overview**

One paragraph. State: this document governs which UI zones are acceptable for ad placement, density rules, slot ID assignments, and consent gating. Phase 5 implementers must read it before writing any ad code. All decisions are locked (CONTEXT.md D-01 through D-07).

**3. Section: ## Ad Zone Table**

Markdown table with columns: Zone | Status | Slot ID | Activation Gate | Mobile Constraint | Density Rule

Row 1 — Station Card Popup:
- Zone: Station Card Popup
- Status: ACTIVE (code exists; `ADS_ENABLED = false` keeps it dormant)
- Slot ID: `3373913657`
- Activation Gate: `ADS_ENABLED = true` AND `VITE_ADSENSE_PUBLISHER_ID` env var set (see Activation Checklist)
- Mobile Constraint: Must not block map, price stats bar, or filter controls. Verify expanded card + ad does not push below stats bar on 375px viewport.
- Density Rule: Max 1 active unit at a time (Leaflet tooltip model — only one card expands at a time)

Row 2 — Full-Page Interstitial:
- Zone: Full-Page Interstitial
- Status: PLANNED — not implemented in Phase 4 or Phase 5 without a contract amendment
- Slot ID: NONE — must be created in AdSense dashboard before implementation
- Activation Gate: Same as Zone 1 — `ADS_ENABLED = true` AND `getAdConsent() === 'accepted'`
- Mobile Constraint: Must render as a dismissible overlay above the map. Must not block map, stats bar, or filter controls. Not a layout-shifting element.
- Density Rule: Once per browser session maximum. No localStorage tracking required at this stage. Trigger TBD in Phase 5 planning.

**4. Section: ## Density Policy**

Two numbered rules:
1. Maximum 1 active ad unit at a time. Only the currently-open station card shows an ad. No simultaneous multi-card ad rendering. The Leaflet tooltip model enforces this naturally — only one expanded card exists at a time. Do not wire ad rendering to override this.
2. Interstitial (when implemented): once per browser session. No per-day tracking required. Session cap must be checked before triggering the interstitial.

**5. Section: ## Consent Gating**

State that consent infrastructure is fully implemented in `src/lib/adConsent.ts` and `src/components/ConsentBanner.tsx`. Do not re-implement it in Phase 5.

Include a table with columns: State | Behavior

Rows:
- `getAdConsent() === null` | ConsentBanner shown. No ad slots render.
- `getAdConsent() === 'refused'` | No ad slots render. Banner dismissed.
- `getAdConsent() === 'accepted'` | `loadAdSense()` called (only if `ADS_ENABLED === true`). Slots render when AdSense confirms fill.
- `ADS_ENABLED === false` | Entire ad system bypassed. No banner, no slots. Current state.

Note: Consent duration is 180 days (Loi 25 compliance), stored in `localStorage` key `'ad-consent'`.

**6. Section: ## Activation Checklist**

Numbered list. These are the steps a Phase 5 implementer must complete to go live with Zone 1:

1. Set `ADS_ENABLED = true` in `src/lib/adConsent.ts` line 2. **Both this constant AND the env var below must be set — neither alone is sufficient.**
2. Set `VITE_ADSENSE_PUBLISHER_ID` in Netlify environment variables (or `.env.local` for dev). The `ADSENSE_CLIENT` constant in `Map.tsx` is derived from this env var; the ad HTML is omitted entirely when it is empty.
3. Rebuild and deploy. Confirm the consent banner appears (Loi 25 requirement).
4. Accept consent in the UI. Confirm `loadAdSense()` injects the AdSense `<script>` into `<head>`.
5. Confirm the station card popup renders the ad slot (`<ins class="adsbygoogle">` with `data-ad-slot="3373913657"`).
6. Verify on a 375px-wide viewport: expanded station card + ad does not obscure the price stats bar.
7. Preserve the CSS gate: `.station-card-ad-container { display: none }` and the `:has(.adsbygoogle[data-ad-status="filled"])` rule in `src/index.css` must remain. **Do not remove or override this gate** — it prevents layout shift from empty ad slots.

For Zone 2 (interstitial): a new slot must be created in the AdSense dashboard first. Slot ID does not yet exist. Do not begin implementation until a slot ID is available and a contract amendment is written for Phase 5.

**7. Section: ## Existing Ad Slot Reference (Zone 1)**

Include the existing `adSlotHtml` template verbatim as a TypeScript code block, sourced from `src/components/Map.tsx` lines 90-101:

```typescript
// src/components/Map.tsx lines 90-101
const adSlotHtml = ADSENSE_CLIENT.trim()
  ? `<div class="station-card-ad-container">
       <div class="station-card-sep"></div>
       <ins class="adsbygoogle"
         style="display:block;width:250px"
         data-ad-client="${esc(ADSENSE_CLIENT)}"
         data-ad-slot="3373913657"
         data-ad-format="auto"
         data-full-width-responsive="true"></ins>
       <div class="station-card-sep"></div>
     </div>`
  : ''
```

Include the CSS gate verbatim as a CSS code block, sourced from `src/index.css` lines 175-182:

```css
/* src/index.css lines 175-182 */
.station-card-ad-container {
  display: none;
}
.station-card-ad-container:has(.adsbygoogle[data-ad-status="filled"]) {
  display: block;
}
```

Brief note: width is 250px fixed. `data-full-width-responsive="true"` is set but the container enforces 250px. Do not modify these values.

**8. Section: ## Common Pitfalls**

Five callout blocks (use `> **Pitfall N:**` format):

Pitfall 1 — Missing ADS_ENABLED: Setting `VITE_ADSENSE_PUBLISHER_ID` alone shows no ads. Both `ADS_ENABLED = true` AND the env var must be set.

Pitfall 2 — Removing the CSS gate: `display: none` on `.station-card-ad-container` and the `:has()` rule prevent layout shift. Do not remove them.

Pitfall 3 — Assuming interstitial slot ID exists: No slot ID exists yet for Zone 2. Create it in the AdSense dashboard before any Phase 5 interstitial work.

Pitfall 4 — Stacking multiple ad units: Multiple open station cards should not each show an ad. The density rule is 1 unit max. The Leaflet model enforces this; do not break it.

Pitfall 5 — Out-of-scope zones: Bottom banner, hamburger menu ad, and sidebar ad are NOT currently acceptable. Do not implement them without a new strategy amendment.

**9. Section: ## Future Considerations**

Brief paragraph followed by a list. State: the zones below were considered during Phase 4 planning. They are NOT currently acceptable placements and must not be implemented without a strategy amendment for a future milestone.

List:
- Bottom banner above price stats bar — considered, not accepted for this milestone
- Ad inside hamburger menu drawer — considered, not accepted
- Sidebar ad — no sidebar component exists

**10. Section: ## Out of Scope**

Explicit list of what this document does NOT cover:
- AdSense account creation or ad unit configuration (done outside codebase)
- Activation of `ADS_ENABLED` (out of scope for Phase 4; Phase 5 concern)
- Interstitial trigger event (to be defined in Phase 5 planning)
- Per-day impression caps (deferred; session cap is sufficient for now)
- Any zone not listed in the Ad Zone Table above

Write the complete document to `.planning/ADS-STRATEGY.md` in a single Write tool call. Do not truncate or summarize any section. Every section listed above must be present and complete.
  </action>

  <verify>
    <automated>grep -c "3373913657" /Users/alainrousseau/Dev/prix-essence-quebec/.planning/ADS-STRATEGY.md && grep -c "ADS_ENABLED" /Users/alainrousseau/Dev/prix-essence-quebec/.planning/ADS-STRATEGY.md && grep -c "VITE_ADSENSE_PUBLISHER_ID" /Users/alainrousseau/Dev/prix-essence-quebec/.planning/ADS-STRATEGY.md && grep -c "not currently acceptable" /Users/alainrousseau/Dev/prix-essence-quebec/.planning/ADS-STRATEGY.md && wc -l /Users/alainrousseau/Dev/prix-essence-quebec/.planning/ADS-STRATEGY.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep "3373913657" .planning/ADS-STRATEGY.md` returns at least 1 match
    - `grep "ADS_ENABLED" .planning/ADS-STRATEGY.md` returns at least 3 matches (zone table, consent section, activation checklist)
    - `grep "VITE_ADSENSE_PUBLISHER_ID" .planning/ADS-STRATEGY.md` returns at least 2 matches (zone table, activation checklist)
    - `grep "not currently acceptable" .planning/ADS-STRATEGY.md` returns at least 1 match (out-of-scope zones)
    - `grep "Station Card Popup" .planning/ADS-STRATEGY.md` returns a match
    - `grep "Full-Page Interstitial" .planning/ADS-STRATEGY.md` returns a match
    - `grep "375px" .planning/ADS-STRATEGY.md` returns a match (mobile constraint verification step)
    - `grep "display: none" .planning/ADS-STRATEGY.md` returns a match (CSS gate pitfall callout)
    - `grep "once per browser session" .planning/ADS-STRATEGY.md` returns a match (interstitial density rule)
    - `wc -l .planning/ADS-STRATEGY.md` shows 80 or more lines
    - Sections present (each grep must return a match):
      - `grep "## Overview" .planning/ADS-STRATEGY.md`
      - `grep "## Ad Zone Table" .planning/ADS-STRATEGY.md`
      - `grep "## Density Policy" .planning/ADS-STRATEGY.md`
      - `grep "## Consent Gating" .planning/ADS-STRATEGY.md`
      - `grep "## Activation Checklist" .planning/ADS-STRATEGY.md`
      - `grep "## Common Pitfalls" .planning/ADS-STRATEGY.md`
      - `grep "## Future Considerations" .planning/ADS-STRATEGY.md`
      - `grep "## Out of Scope" .planning/ADS-STRATEGY.md`
  </acceptance_criteria>

  <done>`.planning/ADS-STRATEGY.md` exists with all 10 required sections, slot ID `3373913657` mapped to Zone 1, mobile constraint (375px) stated, both activation gates documented, out-of-scope zones explicitly marked "not currently acceptable", and CSS gate pitfall included.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Document → Phase 5 executor | The strategy document is consumed as a spec by future Claude agents. Ambiguous or incorrect content will be implemented literally. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | ADS-STRATEGY.md publisher ID reference | accept | Document references env var name only (`VITE_ADSENSE_PUBLISHER_ID`), not the actual publisher ID value. No secret is written to the file. |
| T-04-02 | Tampering | ADS-STRATEGY.md content accuracy | mitigate | Task `<read_first>` mandates reading CONTEXT.md, RESEARCH.md, and UI-SPEC.md before writing. Acceptance criteria use grep checks to verify all critical values are present verbatim. |
| T-04-03 | Repudiation | Out-of-scope zones implemented by Phase 5 | mitigate | Document must use exact phrase "not currently acceptable" for deferred zones. Accepted as an acceptance criterion so the verifier can confirm it. |
</threat_model>

<verification>
After task completion, confirm:

1. File exists: `ls -la .planning/ADS-STRATEGY.md`
2. Slot ID present: `grep "3373913657" .planning/ADS-STRATEGY.md`
3. Both activation gates documented: `grep -c "ADS_ENABLED" .planning/ADS-STRATEGY.md` (expect 3+)
4. Mobile constraint stated: `grep "375px" .planning/ADS-STRATEGY.md`
5. Out-of-scope zones marked: `grep "not currently acceptable" .planning/ADS-STRATEGY.md`
6. Minimum length: `wc -l .planning/ADS-STRATEGY.md` (expect 80+ lines)
7. All 8 required sections present (see acceptance_criteria grep list above)
</verification>

<success_criteria>
Phase 4 is complete when:
1. `.planning/ADS-STRATEGY.md` exists and passes all acceptance criteria grep checks
2. ADS-01: Document specifies two zones (station card active, interstitial planned), max density 1 unit/view, slot ID `3373913657` mapped to Zone 1
3. ADS-02: Document explicitly states no ad zone may block the map, price stats bar, or filter controls — with 375px viewport verification step in the activation checklist
</success_criteria>

<output>
After completion, create `.planning/phases/04-adsense-placement-strategy/04-01-SUMMARY.md` using the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
